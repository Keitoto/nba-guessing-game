// import Head from 'next/head';
// import { useStats } from '../../hooks/useStats';
import { names } from '../../names';
import { ids } from '../../ids';

const Home = () => {
  
  return (
    <>
      <main></main>
    </>
  );
};

export default Home;
